# Предкомпилированные артефакты

[![RU](README.RU.md)](README.RU.md) [![EN](README.md)](README.md)

Эта директория содержит предкомпилированные артефакты сборки для зависимостей **Candle Video** (Flash Attention и Candle Kernels), что позволяет значительно сократить время сборки.

## 📦 Содержимое

Директория содержит артефакты сборки для следующих крейтов:

- **`candle-flash-attn-59809d6940b6f0bb/`**
  - Содержит `libflashattention.a` (~230 МБ) и объектные файлы.
- **`candle-kernels-00d4cbab2c6a62c1/`**
  - Содержит скомпилированные PTX файлы и `libmoe.a`.

## ℹ️ Информация о сборке

Эти артефакты были скомпилированы со следующей конфигурацией:

- **Версия Candle**: 0.9.2-alpha.2
- **Версия CUDA**: 12.6
- **Windows SDK**: 10.0.19041
- **ОС**: Windows

## 🚀 Использование

Эти артефакты предназначены для заполнения директории `target/`, чтобы пропустить перекомпиляцию CUDA-ядер (которая обычно занимает 15-20 минут).

Как использовать:
1. Убедитесь, что ваше локальное окружение соответствует версиям выше.
2. Скопируйте содержимое этих директорий в соответствующие директории `target/debug/build/` или `target/release/build/`, созданные Cargo.
   *Примечание: Имена директорий Cargo содержат хеши (например, `candle-flash-attn-<hash>`). Вам может потребоваться скопировать *содержимое* предкомпилированных папок в ваши локально созданные папки.*

## ⚠️ Предупреждение о совместимости

Эти бинарные артефакты сильно зависят от окружения. Они будут работать только если:
- Вы используете точно те же версии библиотек.
- Ваша версия CUDA Toolkit совпадает.
- Ваша архитектура GPU совместима.

Если вы столкнетесь с ошибками линковки или падениями во время выполнения, удалите эти артефакты из вашей директории `target` и позвольте Cargo пересобрать их из исходников.
